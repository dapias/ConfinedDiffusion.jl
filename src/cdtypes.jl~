type Particle{T}
    r::Array{T,1}
    rprevious::Array{T,1}
end

type Boundary
    shape::Function
    lambda::Float64
        
    function Boundary(shape, lambda)
        s(x) = 1. + lambda*shape(x)
        new(s,lambda)
    end
end
